For each project, add two files matching the names already referenced in lib/data.ts:
  <slug>-poster.jpg   — a still frame shown before hover (portrait, roughly 3:4)
  <slug>-preview.mp4  — a short (5-15s) portrait screen recording, muted, no audio needed

Example for the first project:
  smart-agriculture-poster.jpg
  smart-agriculture-preview.mp4

Keep each video under a few MB (trim/compress) so the site stays fast.
